* Goals
	- [ ] Finish the calculation implementation
	- [ ] Write a lot of tests
	- [ ] Find for parallelization inside the code
	- [ ] Add a macro to build your own scheme at compile time:
	- [ ] Wrap the `Tokens` enum into some trait, so we can pass them
		around 
	- [ ] Build an interface to dynamically construct a scheme at
		runtime, with the values being most likely lazy static

